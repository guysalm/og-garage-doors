# Source material for the rebuild — not part of the site
reference/

# OS / editor cruft
Thumbs.db
Desktop.ini
.DS_Store
*.tmp

# Full-size originals for the review avatars. The repo root is the publish
# root, so anything tracked here is downloadable from the live site - only the
# 96px crops in assets/img belong in it. Regenerate with tools/make-avatars.ps1.
assets/img/Real Photos/People/
